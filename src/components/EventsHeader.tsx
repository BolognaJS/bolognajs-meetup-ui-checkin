import * as React from "react";
import styled from "styled-components";

const EventsHeaderTitle = styled.h1`
    text-align: center;
`

const EventsHeader = () => (
    <EventsHeaderTitle>Meetups checkin</EventsHeaderTitle>
)

export default EventsHeader
